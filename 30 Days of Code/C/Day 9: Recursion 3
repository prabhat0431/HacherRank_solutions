#include<stdio.h>
int factorial(int);
int main()
{
    int n,fact;
    scanf("%d",&n);
    fact=factorial(n);
    printf("%d",fact);
    return 0;
}
int factorial(int n)
{
    if(n>0)
    {
    return n*factorial(n-1);
    }
    else
    return 1;
}
