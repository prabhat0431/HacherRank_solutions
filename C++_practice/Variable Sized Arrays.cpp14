#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
int n,k,q;
cin>>n >> q;
vector<int> a[n];
for(int i=0;i<n;i++)
{
    cin>>k;
    int v;
    for(int j=0;j<k;j++)
    {
        cin>>v;
        a[i].push_back(v);
    }
}
    for(int i=0;i<q;i++)
    {
    int r,s;
    cin>>r>>s;
    cout<<a[r][s]<<endl;
    }
    return 0;
}
